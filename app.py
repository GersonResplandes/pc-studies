import sys

# 1. Otimização de Leitura
input = sys.stdin.readline

# --- Exemplos de uso dentro do problema ---

# Para ler um único número (ex: quantidade de testes)
# n = int(input())

# Para ler uma linha com vários números (ex: coordenadas x y)
# dados = list(map(int, input().split()))